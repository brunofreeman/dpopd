#include "move_models.hpp"

void update_unchanged(Agent **agents, size_t num_agents, InfoUnchanged *info) {
    // does nothing
}

void update_sfm(Agent **agents, size_t num_agents, InfoSFM *info) {
    // TODO
}